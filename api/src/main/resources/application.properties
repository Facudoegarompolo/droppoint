spring.application.name=droppoint-api
server.port=${PORT:8080}

# mock is the default so the MVP runs without external API keys.
droppoint.route-provider=mock
droppoint.google-maps.api-key=${GOOGLE_MAPS_API_KEY:}
droppoint.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:*}
